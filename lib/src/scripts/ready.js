'use strict';

// CLASS

class Outset {
  constructor() {
    document.addEventListener('DOMContentLoaded', this.create.bind(this), false);
  }

  create() {

  }
}

// INSTANCE

var outset = new Outset();
